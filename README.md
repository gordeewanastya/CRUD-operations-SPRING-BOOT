Create table in your database with the following structure: 

```
create table employees_sb(
    id SERIAL PRIMARY KEY,
    first_name varchar(50),
    last_name varchar(50),
    email varchar(40)
);

```